import { createSlice } from "@reduxjs/toolkit";

const borrowSlice = createSlice({
  initialState: {},
  name: "borrow",
  reducers: {
    //    when borrow in done and then imidietly book table copy change
  },
});

// export
export default borrowSlice.reducer;
